image: python:3.7

pipelines:
  default:
    - step:
        caches:
          - pip
        script:
          - pip install -U flit pytest
          - python -m flit install
          - python -m pytest
